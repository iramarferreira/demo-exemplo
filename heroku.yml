setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    DB_DATABASE: ${{DB_DATABASE}}
    DB_HOSTNAME: ${{DB_HOSTNAME}}
    DB_PASS: ${{DB_PASS}}
    DB_PORT: ${{DB_PORT}}
    DB_USER: ${{DB_USER}}

build:
  docker:
    web: Dockerfile
    worker: ./Dockerfile
