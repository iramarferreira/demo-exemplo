setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    config:
      SPRING_DATASOURCE_URL: ${{HEROKU_POSTGRESQL_PURPLE_URL}}
      SPRING_DATASOURCE_USERNAME: ${{DB_USER}}
      SPRING_DATASOURCE_PASSWORD: ${{DB_PASS}}


build:
  docker:
    web: Dockerfile
    worker: ./Dockerfile
